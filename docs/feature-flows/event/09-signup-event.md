# Flow
(setup: applicant login + to an event-detail page)
1. click the register button
2. scenario:
   - success => pop up `success` message => update 
   - no more available slot => pop up `exceed` message