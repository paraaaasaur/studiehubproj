## Domain Issues
- <fixme@2.0.0>: Magic getters `getP_DESC`
- <fixme@1.1.0>: Magic `toString` + usage in `ProductDaoImpl#queryByName`
- <fixme@2.0.0>: search's query logic probably meant to use `AND` in the `WHERE` clause, and `= on p_Type` (except no value maps to %%)